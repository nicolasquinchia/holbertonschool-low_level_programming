#include "holberton.h"
/**
 * add - function that adds two integers and returns the result.
 * @a: integer a to add
 * @b: integer b to add
 *
 * Return: Always 0.
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
